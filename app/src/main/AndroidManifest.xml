<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juank.schroederapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Materiales" />
        <activity android:name=".Materials_Base" />
        <activity android:name=".Room" />
        <activity android:name=".Room_modes" />
        <activity android:name=".MetodosReverbacion"
            android:screenOrientation="landscape">

        </activity>
    </application>

</manifest>